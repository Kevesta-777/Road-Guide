# Relative path works with Vite dev proxy (desktop + Android emulator browser).
VITE_API_BASE_URL=/api/v1

# Headway tileserver style (proxied to localhost:8080 in dev via vite.config.ts).
VITE_MAP_STYLE_URL=/tileserver/styles/basic/style.json
